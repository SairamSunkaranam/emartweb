

#build Workflow

name: webstore-build

on:
   workflow_dispatch:

jobs:
     build:
           name: 'Build and publish our app'
           runs-on: ubuntu-latest

           steps:
               - name: Checkout
                 uses: actions/checkout@v4.2.2

                # - name: restore
                #   uses: dotnet restore

                # - name: checkout
                #   uses: dotnet build --configuration release

                # - name: checkout
                #   uses: dotnet publish --configuration publish
